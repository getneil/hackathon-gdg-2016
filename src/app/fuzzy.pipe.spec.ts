/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FuzzyPipe } from './fuzzy.pipe';

describe('Pipe: Fuzzy', () => {
  it('create an instance', () => {
    let pipe = new FuzzyPipe();
    expect(pipe).toBeTruthy();
  });
});
